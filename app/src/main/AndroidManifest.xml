<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wakafku_dummy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wakafku_dummy">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InputNoPonselActivity" />
        <activity android:name=".VerifikasiPinActivity" />
        <activity android:name=".PinActivity" />
        <activity android:name=".InputDataRegisterActivity" />
        <activity android:name=".KodeOtpActivity" />
        <activity android:name=".LupaPinActivity" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/Theme.Wakafku_dummy1" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.Wakafku_dummy1" />
        <activity android:name=".Onboarding" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>